╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║   🎯 EMPEZAR AQUÍ - SOLUCIÓN PASO A PASO                     ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

⚠️  IMPORTANTE: Olvidémonos del botón por ahora.
    Primero hagamos que funcione MANUALMENTE.

═══════════════════════════════════════════════════════════════

📋 PASO 1: Verificar PostgreSQL client
───────────────────────────────────────────────────────────────

ssh staging
which pg_dump


¿Existe pg_dump?

✅ SÍ → Ir al PASO 2

❌ NO → Instalar:
    sudo dnf install postgresql -y
    
    Luego verificar:
    pg_dump --version


═══════════════════════════════════════════════════════════════

📋 PASO 2: Crear script manual
───────────────────────────────────────────────────────────────

Copiar y pegar TODO este bloque:

cat > ~/sync_manual.sh << 'EOF'
#!/bin/bash
HOST="ls-abe25e9bea57818f0ee32555c0e7b4a10e361535.ctobuhtlkwoj.us-east-1.rds.amazonaws.com"
USER="jveyes"
PASS='a?HC!2.*1#?[==:|289qAI=)#V4kDzl$'
export PGPASSWORD="$PASS"

echo "🔄 Sincronizando..."
pg_dump -h "$HOST" -U "$USER" -d paqueteria_v4 -F c -f /tmp/backup.dump --no-owner --no-acl
echo "✅ Exportado"

pg_restore -h "$HOST" -U "$USER" -d paqueteria_staging /tmp/backup.dump --clean --if-exists --no-owner --no-acl 2>&1 | grep -v "^WARNING" || true
echo "✅ Restaurado"
echo "✅ Completado"
EOF

chmod +x ~/sync_manual.sh


═══════════════════════════════════════════════════════════════

📋 PASO 3: Probar el script
───────────────────────────────────────────────────────────────

~/sync_manual.sh


¿Qué pasó?

✅ Funcionó (muestra "✅ Completado") → ¡PERFECTO! 
   Ahora podemos conectarlo al botón.

❌ Error → Compartir el mensaje de error exacto


═══════════════════════════════════════════════════════════════

🐛 ERRORES COMUNES
───────────────────────────────────────────────────────────────

Error: "pg_dump: command not found"
→ Instalar: sudo dnf install postgresql -y

Error: "connection refused"
→ Verificar conectividad:
  ping ls-abe25e9bea57818f0ee32555c0e7b4a10e361535.ctobuhtlkwoj.us-east-1.rds.amazonaws.com

Error: "authentication failed"
→ Verificar credenciales:
  cat ~/paqueteria-staging/.env.staging | grep POSTGRES


═══════════════════════════════════════════════════════════════

✅ CUANDO FUNCIONE EL SCRIPT MANUAL
───────────────────────────────────────────────────────────────

Avísame y te doy los pasos para conectarlo al botón.

Por ahora, NO toques nada de Docker, contenedores, o código.
Solo haz que este script funcione.


═══════════════════════════════════════════════════════════════

📝 COMANDOS PARA COMPARTIR SI HAY ERROR
───────────────────────────────────────────────────────────────

# 1. Verificar pg_dump
which pg_dump
pg_dump --version

# 2. Probar conexión
export PGPASSWORD='a?HC!2.*1#?[==:|289qAI=)#V4kDzl$'
psql -h ls-abe25e9bea57818f0ee32555c0e7b4a10e361535.ctobuhtlkwoj.us-east-1.rds.amazonaws.com \
     -U jveyes -d paqueteria_v4 -c "SELECT 1;"

# 3. Ejecutar script
~/sync_manual.sh


═══════════════════════════════════════════════════════════════

╔═══════════════════════════════════════════════════════════════╗
║  🎯 OBJETIVO: Que ~/sync_manual.sh funcione                  ║
║  ⏱️  Tiempo: 5 minutos                                        ║
║  📝 Comparte el resultado (éxito o error)                    ║
╚═══════════════════════════════════════════════════════════════╝
