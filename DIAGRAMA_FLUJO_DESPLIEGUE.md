# 🔄 Diagrama de Flujo de Despliegue - PAQUETERÍA v1.0

## 📊 Arquitectura Actual

```
┌─────────────────────────────────────────────────────────────────┐
│                         TU LOCALHOST                             │
│                       (Desarrollo)                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📁 Proyecto Local                                               │
│  ├── CODE/src/                  ← Haces cambios aquí           │
│  ├── .env                       ← Config local (NO se sube)     │
│  ├── .git/                      ← Control de versiones          │
│  └── deploy-to-aws.sh           ← Script de despliegue          │
│                                                                  │
│  🔧 Comandos:                                                    │
│  $ vim CODE/src/app/routes/packages.py                          │
│  $ ./deploy-to-aws.sh "fix: corregir validación"                │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ git push origin main
                         │ (Código + Commits)
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                          GITHUB                                  │
│                    (Repositorio Remoto)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📦 Repositorio: github.com/tu-usuario/paqueteria-v1.0          │
│                                                                  │
│  ✅ Almacena:                                                    │
│  ├── Todo el código fuente                                      │
│  ├── Scripts de despliegue                                      │
│  ├── Documentación                                              │
│  └── Historial de cambios (commits)                             │
│                                                                  │
│  ❌ NO almacena:                                                 │
│  ├── .env (secretos)                                            │
│  ├── logs/                                                      │
│  ├── uploads/                                                   │
│  └── __pycache__/                                               │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ git pull origin main
                         │ (Descarga cambios)
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                      SERVIDOR AWS                                │
│                      (Producción)                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🖥️  EC2 / Lightsail Instance                                   │
│  IP: xx.xx.xx.xx                                                │
│  Dominio: paquetes.com.co                                       │
│                                                                  │
│  📁 /opt/paqueteria/Paqueteria-v1.0/                            │
│  ├── CODE/src/                  ← Código actualizado            │
│  ├── .env                       ← Config producción (diferente) │
│  ├── docker-compose.prod.yml    ← Orquestación                 │
│  └── DOCS/scripts/deployment/   ← Scripts de actualización      │
│                                                                  │
│  🐳 Contenedores Docker:                                         │
│  ├── app (FastAPI)              ← Tu aplicación                 │
│  ├── redis                      ← Cache                         │
│  ├── celery_worker              ← Tareas asíncronas             │
│  ├── celery_beat                ← Tareas programadas            │
│  ├── prometheus                 ← Métricas                      │
│  └── grafana                    ← Dashboards                    │
│                                                                  │
│  🔗 Servicios Externos:                                          │
│  ├── AWS RDS (PostgreSQL)       ← Base de datos                │
│  └── AWS S3                     ← Almacenamiento archivos       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Despliegue Detallado

### Opción 1: Despliegue Automatizado (Recomendado)

```
┌─────────────────────────────────────────────────────────────────┐
│ PASO 1: Desarrollo Local                                        │
└─────────────────────────────────────────────────────────────────┘

  Tu Localhost
  ┌─────────────────────────────────────┐
  │ $ vim CODE/src/app/routes/api.py   │  ← Editas código
  │ $ git status                        │  ← Verificas cambios
  └─────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ PASO 2: Ejecutar Script de Despliegue                           │
└─────────────────────────────────────────────────────────────────┘

  Tu Localhost
  ┌─────────────────────────────────────────────────────────┐
  │ $ ./deploy-to-aws.sh "fix: corregir endpoint API"      │
  └─────────────────────────────────────────────────────────┘
                    ↓
  El script hace automáticamente:
  ┌─────────────────────────────────────────────────────────┐
  │ 1. git add .                                            │
  │ 2. git commit -m "fix: corregir endpoint API"           │
  │ 3. git push origin main                                 │
  │ 4. ssh aws "cd /path && pull-update.sh"                 │
  └─────────────────────────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ PASO 3: GitHub Recibe el Push                                   │
└─────────────────────────────────────────────────────────────────┘

  GitHub
  ┌─────────────────────────────────────────────────────────┐
  │ ✅ Commit recibido                                       │
  │ ✅ Código actualizado en repositorio                     │
  │ ✅ Historial guardado                                    │
  └─────────────────────────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ PASO 4: Servidor AWS Hace Pull                                  │
└─────────────────────────────────────────────────────────────────┘

  Servidor AWS
  ┌─────────────────────────────────────────────────────────┐
  │ $ git fetch origin main                                 │
  │ $ git pull origin main                                  │
  │ ✅ Código actualizado                                    │
  └─────────────────────────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ PASO 5: Análisis Inteligente de Cambios                         │
└─────────────────────────────────────────────────────────────────┘

  Script pull-update.sh analiza:
  ┌─────────────────────────────────────────────────────────┐
  │ ¿Cambió requirements.txt?     → Rebuild necesario      │
  │ ¿Cambió Dockerfile?            → Rebuild necesario      │
  │ ¿Cambió docker-compose.yml?   → Restart necesario      │
  │ ¿Solo cambió código Python?   → Hot reload automático  │
  └─────────────────────────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ PASO 6: Aplicar Cambios                                         │
└─────────────────────────────────────────────────────────────────┘

  Escenario A: Solo código (Python/HTML/CSS/JS)
  ┌─────────────────────────────────────────────────────────┐
  │ ✅ Hot reload detecta cambios                            │
  │ ✅ Uvicorn recarga automáticamente                       │
  │ ⏱️  Tiempo: ~5 segundos                                  │
  │ 🚫 Downtime: NO                                          │
  └─────────────────────────────────────────────────────────┘

  Escenario B: Dependencias o Docker
  ┌─────────────────────────────────────────────────────────┐
  │ 🔨 docker compose build                                  │
  │ 🔄 docker compose restart app                            │
  │ ⏱️  Tiempo: ~2-3 minutos                                 │
  │ 🚫 Downtime: ~30 segundos                                │
  └─────────────────────────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ PASO 7: Verificación Automática                                 │
└─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────┐
  │ ✅ Health check: http://localhost:8000/health            │
  │ ✅ Estado de contenedores: docker compose ps             │
  │ ✅ Logs: docker compose logs --tail=50 app               │
  └─────────────────────────────────────────────────────────┘
                    ↓

┌─────────────────────────────────────────────────────────────────┐
│ ✅ DESPLIEGUE COMPLETADO                                         │
└─────────────────────────────────────────────────────────────────┘

  Tu aplicación está actualizada en:
  🌐 https://paquetes.com.co
```

---

## 🎯 Comparación de Métodos

### Método 1: Script Automatizado (deploy-to-aws.sh)

```
┌──────────────┐
│  LOCALHOST   │  ./deploy-to-aws.sh "mensaje"
└──────┬───────┘
       │ (1 comando hace todo)
       ↓
┌──────────────┐
│   GITHUB     │  ← Push automático
└──────┬───────┘
       │
       ↓
┌──────────────┐
│   AWS        │  ← Pull y actualización automática
└──────────────┘

⏱️  Tiempo total: 30 segundos - 3 minutos
🎯 Complejidad: Baja
✅ Recomendado para: Uso diario
```

### Método 2: Manual Paso a Paso

```
┌──────────────┐
│  LOCALHOST   │  git add . && git commit && git push
└──────┬───────┘
       │ (3 comandos)
       ↓
┌──────────────┐
│   GITHUB     │
└──────┬───────┘
       │
       ↓
┌──────────────┐
│   AWS        │  ssh + cd + ./pull-update.sh
└──────────────┘  (2 comandos más)

⏱️  Tiempo total: 1-5 minutos
🎯 Complejidad: Media
✅ Recomendado para: Cuando necesitas control fino
```

### Método 3: GitHub Actions (CI/CD)

```
┌──────────────┐
│  LOCALHOST   │  git push
└──────┬───────┘
       │
       ↓
┌──────────────┐
│   GITHUB     │  ← Detecta push
│   ACTIONS    │  ← Ejecuta workflow automáticamente
└──────┬───────┘
       │
       ↓
┌──────────────┐
│   AWS        │  ← Despliegue 100% automático
└──────────────┘

⏱️  Tiempo total: 2-5 minutos
🎯 Complejidad: Alta (configuración inicial)
✅ Recomendado para: Equipos grandes, producción crítica
```

---

## 🔍 Análisis de Cambios (pull-update.sh)

```
┌─────────────────────────────────────────────────────────────────┐
│ Script pull-update.sh - Análisis Inteligente                    │
└─────────────────────────────────────────────────────────────────┘

  git diff --name-only HEAD@{1} HEAD
         ↓
  ┌─────────────────────────────────────────────────────────┐
  │ Archivos modificados detectados                         │
  └─────────────────────────────────────────────────────────┘
         ↓
  ┌─────────────────────────────────────────────────────────┐
  │ ¿Qué cambió?                                            │
  ├─────────────────────────────────────────────────────────┤
  │                                                         │
  │ requirements.txt      → REBUILD NECESARIO               │
  │ Dockerfile            → REBUILD NECESARIO               │
  │ docker-compose.yml    → RESTART NECESARIO               │
  │                                                         │
  │ *.py (en CODE/src/)   → HOT RELOAD (automático)         │
  │ *.html                → HOT RELOAD (automático)         │
  │ *.css                 → HOT RELOAD (automático)         │
  │ *.js                  → HOT RELOAD (automático)         │
  │                                                         │
  │ .env                  → RESTART MANUAL (no en Git)      │
  │                                                         │
  └─────────────────────────────────────────────────────────┘
         ↓
  ┌─────────────────────────────────────────────────────────┐
  │ Acción recomendada                                      │
  ├─────────────────────────────────────────────────────────┤
  │                                                         │
  │ Solo código:                                            │
  │   ✅ Nada (hot reload automático)                        │
  │   ⏱️  ~5 segundos                                        │
  │                                                         │
  │ Dependencias/Docker:                                    │
  │   🔨 docker compose build                                │
  │   🔄 docker compose restart                              │
  │   ⏱️  ~2-3 minutos                                       │
  │                                                         │
  └─────────────────────────────────────────────────────────┘
```

---

## 📁 Estructura de Archivos (Qué va dónde)

```
┌─────────────────────────────────────────────────────────────────┐
│ LOCALHOST                                                        │
├─────────────────────────────────────────────────────────────────┤
│ .env                    ← Config LOCAL (desarrollo)             │
│ .git/                   ← Control de versiones                  │
│ CODE/src/               ← Código fuente                         │
│ deploy-to-aws.sh        ← Script de despliegue                  │
│ docker-compose.prod.yml ← Configuración Docker                  │
└─────────────────────────────────────────────────────────────────┘
                    ↓ git push (solo código)
┌─────────────────────────────────────────────────────────────────┐
│ GITHUB                                                           │
├─────────────────────────────────────────────────────────────────┤
│ ✅ CODE/src/             ← Código fuente                         │
│ ✅ deploy-to-aws.sh      ← Scripts                               │
│ ✅ docker-compose.yml    ← Configuración                         │
│ ✅ README.md             ← Documentación                         │
│ ❌ .env                  ← NO (secretos)                         │
│ ❌ logs/                 ← NO (archivos temporales)              │
│ ❌ uploads/              ← NO (archivos de usuarios)             │
└─────────────────────────────────────────────────────────────────┘
                    ↓ git pull
┌─────────────────────────────────────────────────────────────────┐
│ AWS SERVER                                                       │
├─────────────────────────────────────────────────────────────────┤
│ .env                    ← Config PRODUCCIÓN (diferente)         │
│ .git/                   ← Sincronizado con GitHub               │
│ CODE/src/               ← Código actualizado                    │
│ docker-compose.prod.yml ← Configuración Docker                  │
│ uploads/                ← Archivos de usuarios (persistente)    │
│ logs/                   ← Logs de aplicación (persistente)      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Hot Reload en Acción

```
┌─────────────────────────────────────────────────────────────────┐
│ Cómo funciona el Hot Reload                                     │
└─────────────────────────────────────────────────────────────────┘

  1. Código montado como volumen en Docker
  ┌─────────────────────────────────────────────────────────┐
  │ docker-compose.prod.yml:                                │
  │   volumes:                                              │
  │     - ./CODE/src:/app/src  ← Montaje directo            │
  └─────────────────────────────────────────────────────────┘
         ↓
  2. Uvicorn con --reload activo
  ┌─────────────────────────────────────────────────────────┐
  │ command: uvicorn src.main:app --reload                  │
  └─────────────────────────────────────────────────────────┘
         ↓
  3. Cambio detectado
  ┌─────────────────────────────────────────────────────────┐
  │ $ git pull  ← Actualiza archivos en ./CODE/src/         │
  └─────────────────────────────────────────────────────────┘
         ↓
  4. Uvicorn detecta cambio
  ┌─────────────────────────────────────────────────────────┐
  │ INFO: Detected file change in 'routes/api.py'           │
  │ INFO: Reloading...                                      │
  └─────────────────────────────────────────────────────────┘
         ↓
  5. Aplicación recargada
  ┌─────────────────────────────────────────────────────────┐
  │ ✅ Cambios aplicados en ~5 segundos                      │
  │ ✅ Sin downtime                                          │
  │ ✅ Sin rebuild de Docker                                 │
  └─────────────────────────────────────────────────────────┘
```

---

## 📊 Tiempos de Despliegue

```
┌─────────────────────────────────────────────────────────────────┐
│ Tipo de Cambio          │ Tiempo │ Downtime │ Método           │
├─────────────────────────────────────────────────────────────────┤
│ Código Python/HTML/JS   │  30s   │   NO     │ Hot reload       │
│ Templates Jinja2        │  30s   │   NO     │ Hot reload       │
│ CSS/JavaScript          │  30s   │   NO     │ Hot reload       │
│ Configuración .env      │  1m    │   10s    │ Restart manual   │
│ requirements.txt        │  3m    │   30s    │ Rebuild + restart│
│ Dockerfile              │  3m    │   30s    │ Rebuild + restart│
│ docker-compose.yml      │  2m    │   20s    │ Restart          │
└─────────────────────────────────────────────────────────────────┘

Leyenda:
  ⚡ Hot reload: Cambios instantáneos sin reiniciar
  🔄 Restart: Reinicio rápido del contenedor
  🔨 Rebuild: Reconstrucción completa de la imagen
```

---

**Creado:** $(date)
**Versión:** 1.0.0
